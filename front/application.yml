spring:
  cloud:
    openfeign:
      okhttp:
        enabled: true
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false
gateway-base-url: http://192.168.1.191:10250
feign:
  url:
    auth: "default"
    shop: "default"
management:
  endpoints:
    web:
      exposure:
        include: refresh, health, info
